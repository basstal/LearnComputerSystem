#include<stdio.h>

/**
 * 每个表达式最多3个运算，将整数变量x乘以不同的常数因子k。
 * 只使用+、-和<<运算。
 */

int main(void)
{
    int x = 15;
    // k = 17
    x = (x << 4) + x;
    
    // k = -7
    x = - ((x << 3) - 1);

    // k = 60
    x = (x << 6) - (x << 2);

    // k = -112
    x = (x << 4) - (x << 7);
}