#include <stdio.h>

/**
 * 计算x和y采用补码形式的情况下，x乘y的高w位（无符号数位参数）。可以使用原型：
 * int signed_high_prod(int x, int y);
 * 来计算有符号数的x乘y的高w位
 */

unsigned unsigned_high_prod(unsigned x, unsigned y){
    return 0;    
}